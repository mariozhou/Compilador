mi_camara.Initialize(Camera{  

 Import paquete1.clase;
 Import paquete2.tool;

 Text txt;
 Int int = 15 ; 
 Int int2 = 0; 
 Float real = 2.0;
 Bool bool = False; 

 Camera.On();

 

 For (Int i = 0; i>12; i++){
		Avanzar(10,5);
	}
	If(i>7){
	Camera.Move('left',5,270);
     If(Camera.IsMove() == False){
		Camera.Move('Right',5,1);
		}
	}
 While(Micro.IsOn() == True){
	RecordTime(15);
	 Display 'Camera encedida';
 }

	Camera.Off();
 




});